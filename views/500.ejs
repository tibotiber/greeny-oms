<!DOCTYPE html>
<div id="page-login" class="row">
  <div class="col-xs-12 col-md-8 col-md-offset-2 col-sm-8 col-sm-offset-2">
    <div class="box">
      <div class="box-content">
	<div class="text-center">
	  <h3 class="page-header">Error 500 - Internal Server Error</h3>
	</div>
	<div>
	  <% if (typeof errors !== 'undefined') { %>
          <pre class="error"><code>
              <% _.each(errors, function (error) { %>
              <%= error %>
              <% }); %>
          </code></pre>
          <% } else { %>
	  <p class="error">
            Sorry, an error has occured while processing your request.
          </p>
	  <% } %>
	</div> 
	<div class="text-center">
	  <a href="/help" class="btn btn-primary">Help us report this bug!</a>
	</div>
      </div>
    </div>
  </div>
</div>
