import {
  sailsPath
} from '../../../helpers/sails-path';
import { module, test } from 'qunit';

module('SailsPathHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = sailsPath(42);
  assert.ok(result);
});
